example_codes:
  - title: Python script to read file
    content: Write a python script to read file
  - title: Write to_dict function
    content: |
      Write the `to_dict()` function to convert the object to json serializable

      ```python
      class User(BaseModel):
          name: str
          picture: str

          def to_dict():
      ```
  - title: Explain Rust code
    content: |
      Explain the Rust code below

      ```rust
      use actix_web::{get, Responder};
      use firebase_auth::FirebaseUser;

      #[get("/hello")]
      async fn greet(user: FirebaseUser) -> impl Responder {
          let email = user.email.unwrap_or("empty email".to_string());
          format!("Hello {}!", email)
      }
      ```
  - title: Haskell explain
    content: |
      Explain the Haskell code below

      ```haskell
      getPetNickname :: Maybe String
      getPetNickname = getUser >> getPet >> getPetName
      ```
  - title: What is Monad
    content: |
      Explain Monad in easy to understand. Give some example codes in Haskell.
